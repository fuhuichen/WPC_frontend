export enum EPageStep {
    table = 'table',
    form = 'form',
    none = 'none',
}

export enum EPageAction {
    list = 'list',
    create = 'create',
    edit = 'edit',
    preview = 'preview',
    changePassword = 'changePassword',
    none = 'none',
}
